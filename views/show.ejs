<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>users</title>
    <style>
        tr,td,th{border: 1px solid black;}
    </style>
</head>
<body>
    <table>
        <tr>
            <th>id</th>
            <th>username</th>
            <th>email</th>
        </tr>
        <% for(user of users){ %>  
        <tr>
            <td><%= user.id %></td>
            <td><%= user.username %></td>
            <td><%= user.email %></td>
            <td>
     <form method="get" action="show/<%= user.id %>/edit"><button>Edit</button></form>
            </td>
            <td>
                <form method="get" action="show/<% user.id %>?_method=DELETE"><button>delete</button></form>
            </td>
            </tr>
        <% } %>

    </table>
</body>
</html>